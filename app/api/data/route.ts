import { NextResponse } from 'next/server';
import { sql } from '@/lib/db';
export const dynamic='force-dynamic'; export const runtime='nodejs';
const PAYMENTS=['Tunai','QR / Online Transfer','Kad'] as const; type Payment=(typeof PAYMENTS)[number]; type CartInput={productId:number;quantity:number};
function asPositiveInt(v:unknown){const n=Number(v);return Number.isInteger(n)&&n>0?n:null}function asNonNegativeNumber(v:unknown,f=0){const n=Number(v??f);return Number.isFinite(n)&&n>=0?n:null}function normalizeItems(v:unknown):CartInput[]|null{if(!Array.isArray(v)||!v.length)return null;const items=v.map(i=>({productId:asPositiveInt(i?.productId),quantity:asPositiveInt(i?.quantity)}));if(items.some(i=>i.productId===null||i.quantity===null))return null;return items as CartInput[]}function isPayment(v:unknown):v is Payment{return typeof v==='string'&&PAYMENTS.includes(v as Payment)}function errorMessage(e:unknown){const x=e as{code?:string;message?:string};if(x?.code==='23505')return'SKU atau Receipt No. sudah wujud.';return x?.message||'Ralat pangkalan data.'}
export async function GET(){try{const[products,transactions,movements]=await Promise.all([sql`SELECT id,name,sku,category,price::float8 AS price,cost::float8 AS cost,stock,low_stock AS "lowStock" FROM products ORDER BY name ASC`,sql`SELECT t.receipt_no AS id,t.receipt_no AS "receiptNo",t.sold_at AS date,t.total::float8 AS total,t.payment,t.note,COALESCE(json_agg(json_build_object('productId',ti.product_id,'quantity',ti.quantity,'unitPrice',ti.unit_price::float8,'subtotal',ti.subtotal::float8) ORDER BY ti.id) FILTER(WHERE ti.id IS NOT NULL),'[]'::json) AS items FROM transactions t LEFT JOIN transaction_items ti ON ti.transaction_id=t.id GROUP BY t.id ORDER BY t.sold_at DESC`,sql`SELECT id,product_id AS "productId",quantity,movement_type AS "movementType",transaction_id AS "transactionId",moved_at AS date,note FROM stock_movements ORDER BY moved_at DESC LIMIT 500`]);return NextResponse.json({products,transactions,movements,serverTime:new Date().toISOString()})}catch(e){return NextResponse.json({error:errorMessage(e)},{status:500})}}
export async function POST(request:Request){try{const body=await request.json();const action=String(body?.action??'');if(action==='product'){const name=String(body.name??'').trim(),sku=String(body.sku??'').trim().toUpperCase(),category=String(body.category??'Squishy').trim()||'Squishy';const price=asNonNegativeNumber(body.price),cost=asNonNegativeNumber(body.cost),stock=asNonNegativeNumber(body.stock),lowStock=asNonNegativeNumber(body.lowStock,5);if(!name||!sku||price===null||price<=0||cost===null||stock===null||lowStock===null)return NextResponse.json({error:'Maklumat produk tidak sah.'},{status:400});const[p]=await sql`WITH created AS(INSERT INTO products(name,sku,category,price,cost,stock,low_stock) VALUES(${name},${sku},${category},${price},${cost},${Math.trunc(stock)},${Math.trunc(lowStock)}) RETURNING id,name,sku,category,price,cost,stock,low_stock),movement AS(INSERT INTO stock_movements(product_id,quantity,movement_type,note) SELECT id,stock,'STOCK_IN','Initial stock' FROM created WHERE stock>0 RETURNING id) SELECT id,name,sku,category,price::float8 AS price,cost::float8 AS cost,stock,low_stock AS "lowStock" FROM created`;return NextResponse.json(p,{status:201})}
if(action==='stock-in'){const productId=asPositiveInt(body.productId),quantity=asPositiveInt(body.quantity),note=String(body.note??'Restock').trim()||'Restock';if(!productId||!quantity)return NextResponse.json({error:'Produk atau kuantiti tidak sah.'},{status:400});const[p]=await sql`WITH locked AS MATERIALIZED(SELECT id FROM products WHERE id=${productId} FOR UPDATE),updated AS(UPDATE products p SET stock=p.stock+${quantity},updated_at=NOW() FROM locked l WHERE p.id=l.id RETURNING p.id,p.name,p.sku,p.category,p.price,p.cost,p.stock,p.low_stock),movement AS(INSERT INTO stock_movements(product_id,quantity,movement_type,note) SELECT id,${quantity},'STOCK_IN',${note} FROM updated RETURNING id) SELECT id,name,sku,category,price::float8 AS price,cost::float8 AS cost,stock,low_stock AS "lowStock" FROM updated`;return p?NextResponse.json(p):NextResponse.json({error:'Produk tidak dijumpai.'},{status:404})}
if(action==='checkout'){const items=normalizeItems(body.items),payment=body.payment,note=String(body.note??'').trim();if(!items)return NextResponse.json({error:'Cart kosong atau item tidak sah.'},{status:400});if(!isPayment(payment))return NextResponse.json({error:'Kaedah pembayaran tidak sah.'},{status:400});const payload=JSON.stringify(items);const[t]=await sql`WITH input AS(SELECT "productId" AS product_id,SUM(quantity)::int AS quantity FROM jsonb_to_recordset(${payload}::jsonb) AS x("productId" bigint,quantity int) GROUP BY "productId"),ps AS MATERIALIZED(SELECT p.id,p.price,p.stock,i.quantity FROM products p JOIN input i ON i.product_id=p.id FOR UPDATE OF p),valid AS(SELECT (SELECT COUNT(*) FROM input)>0 AND (SELECT COUNT(*) FROM input)=COUNT(*) AND COALESCE(bool_and(quantity>0 AND stock>=quantity),false) ok FROM ps),counter AS(INSERT INTO receipt_counters(sale_date,next_no) SELECT (NOW() AT TIME ZONE 'Asia/Kuala_Lumpur')::date,2 FROM valid WHERE ok ON CONFLICT(sale_date) DO UPDATE SET next_no=receipt_counters.next_no+1 RETURNING sale_date,next_no-1 seq),tx AS(INSERT INTO transactions(receipt_no,sold_at,total,payment,note) SELECT 'SQ-'||to_char(c.sale_date,'YYYYMMDD')||'-'||lpad(c.seq::text,4,'0'),NOW(),SUM(ps.price*ps.quantity),${payment},${note} FROM counter c CROSS JOIN ps GROUP BY c.sale_date,c.seq RETURNING id,receipt_no,sold_at,total,payment,note),ir AS(INSERT INTO transaction_items(transaction_id,product_id,quantity,unit_price,subtotal) SELECT tx.id,ps.id,ps.quantity,ps.price,ps.price*ps.quantity FROM tx CROSS JOIN ps RETURNING product_id,quantity,unit_price,subtotal),su AS(UPDATE products p SET stock=p.stock-ps.quantity,updated_at=NOW() FROM ps,valid v WHERE v.ok AND p.id=ps.id RETURNING p.id),mr AS(INSERT INTO stock_movements(product_id,quantity,movement_type,transaction_id,note) SELECT ps.id,-ps.quantity,'SALE',tx.id,COALESCE(NULLIF(${note},''),'Sale') FROM tx CROSS JOIN ps RETURNING id) SELECT tx.receipt_no id,tx.receipt_no AS "receiptNo",tx.sold_at date,tx.total::float8 total,tx.payment,tx.note,COALESCE((SELECT json_agg(json_build_object('productId',ir.product_id,'quantity',ir.quantity,'unitPrice',ir.unit_price::float8,'subtotal',ir.subtotal::float8) ORDER BY ir.product_id) FROM ir),'[]'::json) items FROM tx`;return t?NextResponse.json(t,{status:201}):NextResponse.json({error:'Stok tidak mencukupi atau item tidak sah.'},{status:409})}
if(action==='update-transaction'){const receiptNo=String(body.receiptNo??'').trim(),items=normalizeItems(body.items),payment=body.payment,note=String(body.note??'').trim();if(!receiptNo||!items||!isPayment(payment))return NextResponse.json({error:'Transaksi tidak sah.'},{status:400});const payload=JSON.stringify(items);const[t]=await sql`WITH target AS MATERIALIZED(SELECT id,receipt_no,sold_at FROM transactions WHERE receipt_no=${receiptNo} FOR UPDATE),old_items AS(SELECT ti.product_id,SUM(ti.quantity)::int quantity FROM transaction_items ti JOIN target t ON t.id=ti.transaction_id GROUP BY ti.product_id),input AS(SELECT "productId" product_id,SUM(quantity)::int quantity FROM jsonb_to_recordset(${payload}::jsonb) AS x("productId" bigint,quantity int) GROUP BY "productId"),all_ids AS(SELECT product_id FROM old_items UNION SELECT product_id FROM input),lp AS MATERIALIZED(SELECT p.id,p.price,p.stock FROM products p JOIN all_ids a ON a.product_id=p.id FOR UPDATE OF p),ps AS(SELECT lp.id,lp.price,lp.stock,COALESCE(i.quantity,0) new_qty,COALESCE(o.quantity,0) old_qty FROM lp LEFT JOIN input i ON i.product_id=lp.id LEFT JOIN old_items o ON o.product_id=lp.id),valid AS(SELECT EXISTS(SELECT 1 FROM target) AND (SELECT COUNT(*) FROM input)>0 AND (SELECT COUNT(*) FROM input)=(SELECT COUNT(*) FROM lp JOIN input i ON i.product_id=lp.id) AND COALESCE(bool_and(CASE WHEN new_qty>0 THEN stock+old_qty>=new_qty ELSE true END),false) ok FROM ps),sa AS(UPDATE products p SET stock=p.stock+ps.old_qty-ps.new_qty,updated_at=NOW() FROM ps,valid v WHERE v.ok AND p.id=ps.id RETURNING p.id),di AS(DELETE FROM transaction_items WHERE transaction_id=(SELECT id FROM target) AND (SELECT ok FROM valid) RETURNING product_id),ut AS(UPDATE transactions t SET total=x.total,payment=${payment},note=${note} FROM(SELECT SUM(price*new_qty) total FROM ps WHERE new_qty>0)x,valid v WHERE v.ok AND t.id=(SELECT id FROM target) RETURNING t.id,t.receipt_no,t.sold_at,t.total,t.payment,t.note),ii AS(INSERT INTO transaction_items(transaction_id,product_id,quantity,unit_price,subtotal) SELECT ut.id,ps.id,ps.new_qty,ps.price,ps.price*ps.new_qty FROM ut CROSS JOIN ps WHERE ps.new_qty>0 RETURNING product_id,quantity,unit_price,subtotal),rm AS(INSERT INTO stock_movements(product_id,quantity,movement_type,transaction_id,note) SELECT o.product_id,o.quantity,'ADJUSTMENT',ut.id,'Edit transaction: restore previous sale' FROM old_items o CROSS JOIN ut RETURNING id),sm AS(INSERT INTO stock_movements(product_id,quantity,movement_type,transaction_id,note) SELECT ps.id,-ps.new_qty,'SALE',ut.id,'Edited sale' FROM ps CROSS JOIN ut WHERE ps.new_qty>0 RETURNING id) SELECT ut.receipt_no id,ut.receipt_no AS "receiptNo",ut.sold_at date,ut.total::float8 total,ut.payment,ut.note,COALESCE((SELECT json_agg(json_build_object('productId',ii.product_id,'quantity',ii.quantity,'unitPrice',ii.unit_price::float8,'subtotal',ii.subtotal::float8) ORDER BY ii.product_id) FROM ii),'[]'::json) items FROM ut`;return t?NextResponse.json(t):NextResponse.json({error:'Transaksi tidak dijumpai atau stok tidak mencukupi.'},{status:409})}
if(action==='delete-transaction'){const receiptNo=String(body.receiptNo??'').trim();if(!receiptNo)return NextResponse.json({error:'Receipt No. diperlukan.'},{status:400});const[d]=await sql`WITH target AS MATERIALIZED(SELECT id,receipt_no FROM transactions WHERE receipt_no=${receiptNo} FOR UPDATE),rq AS(SELECT ti.product_id,SUM(ti.quantity)::int quantity FROM transaction_items ti JOIN target t ON t.id=ti.transaction_id GROUP BY ti.product_id),lp AS MATERIALIZED(SELECT p.id FROM products p JOIN rq r ON r.product_id=p.id FOR UPDATE OF p),restored AS(UPDATE products p SET stock=p.stock+r.quantity,updated_at=NOW() FROM rq r,lp WHERE p.id=r.product_id AND lp.id=p.id RETURNING p.id),movements AS(INSERT INTO stock_movements(product_id,quantity,movement_type,transaction_id,note) SELECT r.product_id,r.quantity,'ADJUSTMENT',t.id,'Transaction deleted: stock restored' FROM rq r CROSS JOIN target t RETURNING id),gone AS(DELETE FROM transactions t WHERE t.id=(SELECT id FROM target) RETURNING t.receipt_no) SELECT receipt_no AS "receiptNo" FROM gone`;return d?NextResponse.json({ok:true,...d}):NextResponse.json({error:'Transaksi tidak dijumpai.'},{status:404})}return NextResponse.json({error:'Action tidak disokong.'},{status:400})}catch(e){return NextResponse.json({error:errorMessage(e)},{status:500})}}
