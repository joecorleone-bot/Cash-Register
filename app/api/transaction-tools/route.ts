import { NextResponse } from 'next/server';
import { sql } from '@/lib/db';
export const dynamic='force-dynamic';export const runtime='nodejs';
function positiveInt(v:unknown){const n=Number(v);return Number.isInteger(n)&&n>0?n:null}function nonNegativeNumber(v:unknown){const n=Number(v);return Number.isFinite(n)&&n>=0?n:null}function errorMessage(e:unknown){const x=e as{code?:string;message?:string};if(x?.code==='23505')return'SKU sudah digunakan oleh produk lain.';return x?.message||'Ralat pangkalan data.'}
export async function POST(request:Request){try{const body=await request.json();const action=String(body?.action??'');
if(action==='edit-product'){const productId=positiveInt(body.productId),name=String(body.name??'').trim(),sku=String(body.sku??'').trim().toUpperCase(),category=String(body.category??'Squishy').trim()||'Squishy';const price=nonNegativeNumber(body.price),cost=nonNegativeNumber(body.cost),stock=nonNegativeNumber(body.stock),lowStock=nonNegativeNumber(body.lowStock);if(!productId||!name||!sku||sku.startsWith('ARCHIVED-')||price===null||price<=0||cost===null||stock===null||lowStock===null)return NextResponse.json({error:'Maklumat produk tidak sah.'},{status:400});const nextStock=Math.trunc(stock),nextLowStock=Math.trunc(lowStock);const[p]=await sql`WITH target AS MATERIALIZED(SELECT id,stock FROM products WHERE id=${productId} AND sku NOT LIKE 'ARCHIVED-%' FOR UPDATE),movement AS(INSERT INTO stock_movements(product_id,quantity,movement_type,note) SELECT id,${nextStock}-stock,'ADJUSTMENT','Inventory product edited: stock adjusted' FROM target WHERE ${nextStock}<>stock RETURNING id),updated AS(UPDATE products p SET name=${name},sku=${sku},category=${category},price=${price},cost=${cost},stock=${nextStock},low_stock=${nextLowStock},updated_at=NOW() FROM target t WHERE p.id=t.id RETURNING p.id,p.name,p.sku,p.category,p.price,p.cost,p.stock,p.low_stock) SELECT id,name,sku,category,price::float8 AS price,cost::float8 AS cost,stock,low_stock AS "lowStock" FROM updated`;return p?NextResponse.json(p):NextResponse.json({error:'Produk tidak dijumpai atau sudah diarkib.'},{status:404})}
if(action==='archive-product'){const productId=positiveInt(body.productId);if(!productId)return NextResponse.json({error:'Produk tidak sah.'},{status:400});const[p]=await sql`WITH target AS MATERIALIZED(SELECT id,sku,stock FROM products WHERE id=${productId} AND sku NOT LIKE 'ARCHIVED-%' FOR UPDATE),movement AS(INSERT INTO stock_movements(product_id,quantity,movement_type,note) SELECT id,-stock,'ADJUSTMENT','Product archived from active inventory' FROM target WHERE stock<>0 RETURNING id),updated AS(UPDATE products p SET sku='ARCHIVED-'||p.id::text||'-'||p.sku,stock=0,updated_at=NOW() FROM target t WHERE p.id=t.id RETURNING p.id,p.name,p.sku) SELECT * FROM updated`;return p?NextResponse.json(p):NextResponse.json({error:'Produk tidak dijumpai atau sudah diarkib.'},{status:404})}
if(action==='apply-discount'){const receiptNo=String(body.receiptNo??'').trim(),discountAmount=nonNegativeNumber(body.discountAmount);if(!receiptNo||discountAmount===null)return NextResponse.json({error:'Discount tidak sah.'},{status:400});const[t]=await sql`WITH gross AS(SELECT t.id,t.receipt_no,COALESCE(SUM(ti.subtotal),0)::numeric subtotal FROM transactions t LEFT JOIN transaction_items ti ON ti.transaction_id=t.id WHERE t.receipt_no=${receiptNo} GROUP BY t.id),updated AS(UPDATE transactions t SET total=GREATEST(g.subtotal-LEAST(${discountAmount},g.subtotal),0) FROM gross g WHERE t.id=g.id RETURNING t.id,t.receipt_no,t.sold_at,t.total,t.payment,t.note) SELECT u.receipt_no id,u.receipt_no AS "receiptNo",u.sold_at date,u.total::float8 total,u.payment,u.note,COALESCE((SELECT json_agg(json_build_object('productId',ti.product_id,'quantity',ti.quantity,'unitPrice',ti.unit_price::float8,'subtotal',ti.subtotal::float8) ORDER BY ti.id) FROM transaction_items ti WHERE ti.transaction_id=u.id),'[]'::json) items FROM updated u`;return t?NextResponse.json(t):NextResponse.json({error:'Transaksi tidak dijumpai.'},{status:404})}return NextResponse.json({error:'Action tidak dikenali.'},{status:400})}catch(e){return NextResponse.json({error:errorMessage(e)},{status:500})}}
