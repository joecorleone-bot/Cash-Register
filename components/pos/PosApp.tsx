"use client";
import { useCallback, useEffect, useMemo, useState } from 'react';
import { BarChart3, LayoutDashboard, Package, RefreshCw, ShoppingBag, Sparkles } from 'lucide-react';
import DashboardView from './DashboardView'; import InventoryView from './InventoryView'; import PosView from './PosView'; import ReportsView from './ReportsView'; import ReceiptModal from './ReceiptModal'; import { AddProductModal, EditProductModal, StockInModal } from './InventoryModals';
import { exportTransactionsCsv, posApi, posToolsApi } from '@/lib/pos-client'; import type { SessionUser } from '@/lib/auth'; import type { CartItem, Payment, Product, Transaction, View } from '@/lib/pos-types';
type DiscountType='percent'|'fixed'; type ProductFormValue={name:string;sku:string;category:string;price:number;cost:number;stock:number;lowStock:number}; type CashSummary={received:number;change:number}|null; const emptyProduct=():ProductFormValue=>({name:'',sku:'',category:'Squishy',price:0,cost:0,stock:0,lowStock:5});
export default function PosApp({currentUser}:{currentUser:SessionUser}){const isAdmin=currentUser.role==='admin';const[view,setView]=useState<View>(isAdmin?'dashboard':'pos');const[products,setProducts]=useState<Product[]>([]);const[transactions,setTransactions]=useState<Transaction[]>([]);const[cart,setCart]=useState<CartItem[]>([]);const[payment,setPayment]=useState<Payment>('Tunai');const[note,setNote]=useState('');const[discountType,setDiscountType]=useState<DiscountType>('percent');const[discountValue,setDiscountValue]=useState(0);const[cashReceived,setCashReceived]=useState(0);const[editing,setEditing]=useState<Transaction|null>(null);const[receipt,setReceipt]=useState<Transaction|null>(null);const[receiptCash,setReceiptCash]=useState<CashSummary>(null);const[loading,setLoading]=useState(true);const[saving,setSaving]=useState(false);const[error,setError]=useState('');const[query,setQuery]=useState('');const[dateFilter,setDateFilter]=useState('');const[productQuery,setProductQuery]=useState('');const[addProductOpen,setAddProductOpen]=useState(false);const[editProductOpen,setEditProductOpen]=useState<Product|null>(null);const[editProductValue,setEditProductValue]=useState<ProductFormValue>(emptyProduct());const[stockOpen,setStockOpen]=useState<Product|null>(null);const[stockQty,setStockQty]=useState(1);const[stockNote,setStockNote]=useState('Restock');const[newProduct,setNewProduct]=useState<ProductFormValue>(emptyProduct());
const loadData=useCallback(async()=>{try{setError('');const data=await posApi();setProducts((data.products||[]).map((p:Product)=>({...p,category:p.category||'Squishy',id:Number(p.id),price:Number(p.price),cost:Number(p.cost),stock:Number(p.stock),lowStock:Number(p.lowStock)})));setTransactions((data.transactions||[]).map((t:Transaction)=>({...t,total:Number(t.total),items:(t.items||[]).map(i=>({...i,productId:Number(i.productId),quantity:Number(i.quantity),unitPrice:Number(i.unitPrice)}))})));}catch(e){setError(e instanceof Error?e.message:'Gagal memuatkan data.')}finally{setLoading(false)}},[]);useEffect(()=>{loadData()},[loadData]);
const productMap=useMemo(()=>new Map(products.map(p=>[p.id,p])),[products]);const activeProducts=useMemo(()=>products.filter(p=>!p.sku.startsWith('ARCHIVED-')),[products]);const oldQty=(id:number)=>editing?.items.find(i=>i.productId===id)?.quantity||0;const available=(id:number)=>(productMap.get(id)?.stock||0)+oldQty(id);const subtotal=cart.reduce((s,i)=>s+(productMap.get(i.productId)?.price||0)*i.quantity,0);const rawDiscount=discountType==='percent'?subtotal*Math.min(Math.max(discountValue,0),100)/100:Math.max(discountValue,0);const discountAmount=Math.min(rawDiscount,subtotal);const total=Math.max(subtotal-discountAmount,0);
function resetSale(){setEditing(null);setCart([]);setPayment('Tunai');setNote('');setDiscountType('percent');setDiscountValue(0);setCashReceived(0)}function addToCart(id:number){const max=available(id);if(max<=0)return;setCart(c=>{const f=c.find(i=>i.productId===id);if(!f)return[...c,{productId:id,quantity:1}];if(f.quantity>=max)return c;return c.map(i=>i.productId===id?{...i,quantity:i.quantity+1}:i)})}function changeQty(id:number,d:number){const max=available(id);setCart(c=>c.map(i=>i.productId===id?{...i,quantity:Math.min(max,Math.max(0,i.quantity+d))}:i).filter(i=>i.quantity>0))}
async function checkout(){if(!cart.length||saving)return;if(payment==='Tunai'&&cashReceived<total){setError('Cash received tidak mencukupi untuk jumlah pembayaran.');return}try{setSaving(true);setError('');const cashSnapshot=payment==='Tunai'?{received:cashReceived,change:Math.max(cashReceived-total,0)}:null;const baseTx=await posApi(editing?{action:'update-transaction',receiptNo:editing.id,items:cart,payment,note}:{action:'checkout',items:cart,payment,note});const tx=discountAmount>0?await posToolsApi({action:'apply-discount',receiptNo:baseTx.id,discountAmount}):baseTx;await loadData();setReceiptCash(cashSnapshot);setReceipt(tx);resetSale()}catch(e){setError(e instanceof Error?e.message:'Checkout gagal.')}finally{setSaving(false)}}
function editTransaction(tx:Transaction){if(!isAdmin)return;setEditing(tx);setCart(tx.items.map(i=>({productId:i.productId,quantity:i.quantity})));setPayment(tx.payment);setNote(tx.note||'');const gross=tx.items.reduce((s,i)=>s+i.unitPrice*i.quantity,0);setDiscountType('fixed');setDiscountValue(Math.max(gross-tx.total,0));setCashReceived(tx.payment==='Tunai'?tx.total:0);setView('pos');window.scrollTo({top:0,behavior:'smooth'})}function openProductEditor(p:Product){if(!isAdmin)return;setEditProductOpen(p);setEditProductValue({name:p.name,sku:p.sku,category:p.category||'Squishy',price:p.price,cost:p.cost,stock:p.stock,lowStock:p.lowStock})}
async function saveProductEdit(){if(!isAdmin||!editProductOpen||!editProductValue.name.trim()||!editProductValue.sku.trim()||editProductValue.price<=0)return;try{setSaving(true);setError('');await posToolsApi({action:'edit-product',productId:editProductOpen.id,...editProductValue});await loadData();setEditProductOpen(null)}catch(e){setError(e instanceof Error?e.message:'Gagal edit produk.')}finally{setSaving(false)}}async function deleteTransaction(tx:Transaction){if(!isAdmin||!confirm(`Padam transaksi ${tx.id}? Stok akan dipulangkan.`))return;try{setSaving(true);await posApi({action:'delete-transaction',receiptNo:tx.id});await loadData()}catch(e){setError(e instanceof Error?e.message:'Gagal padam transaksi.')}finally{setSaving(false)}}async function addProduct(){if(!isAdmin||!newProduct.name.trim()||!newProduct.sku.trim()||newProduct.price<=0)return;try{setSaving(true);await posApi({action:'product',...newProduct});await loadData();setNewProduct(emptyProduct());setAddProductOpen(false)}catch(e){setError(e instanceof Error?e.message:'Gagal tambah produk.')}finally{setSaving(false)}}async function stockIn(){if(!isAdmin||!stockOpen||stockQty<=0)return;try{setSaving(true);await posApi({action:'stock-in',productId:stockOpen.id,quantity:stockQty,note:stockNote});await loadData();setStockOpen(null);setStockQty(1)}finally{setSaving(false)}}async function archiveProduct(p:Product){if(!isAdmin||!confirm(`Archive ${p.name}? Produk akan disembunyikan daripada POS dan baki stok menjadi 0.`))return;try{setSaving(true);await posToolsApi({action:'archive-product',productId:p.id});await loadData()}finally{setSaving(false)}}function closeReceipt(){setReceipt(null);setReceiptCash(null)}function newSaleFromReceipt(){closeReceipt();resetSale();setView('pos')}const title=view==='dashboard'?'Sales Dashboard':view==='pos'?'Cash Register':view==='inventory'?'Inventory':'Daily Reports';
return <main className="posAppShell"><aside className="posSidebar"><div className="posBrand"><div className="posBrandMark"><Sparkles size={20}/></div><div><b>LITTLE SQUISHY</b><span>Play • Sell • Smile</span></div></div><nav>{isAdmin&&<button className={view==='dashboard'?'active':''} onClick={()=>setView('dashboard')}><LayoutDashboard size={18}/>Dashboard</button>}<button className={view==='pos'?'active':''} onClick={()=>setView('pos')}><ShoppingBag size={18}/>Sales / POS</button>{isAdmin&&<button className={view==='inventory'?'active':''} onClick={()=>setView('inventory')}><Package size={18}/>Inventory</button>}{isAdmin&&<button className={view==='reports'?'active':''} onClick={()=>setView('reports')}><BarChart3 size={18}/>Daily Reports</button>}</nav><div className="posDbStatus"><span className={loading?'posDot busy':'posDot'}/>{loading?'Connecting...':`${isAdmin?'Admin':'Cashier'} • Neon live`}</div></aside><section className="posMainArea"><header className="posTopbar"><div><p>{isAdmin?'Admin workspace':'Cashier workspace'} ✨</p><h1>{title}</h1></div><button className="posSecondaryButton" onClick={loadData}><RefreshCw size={16}/>Refresh</button></header>{error&&<div className="posAlert"><span>{error}</span><button onClick={()=>setError('')}>×</button></div>}{view==='dashboard'&&isAdmin&&<DashboardView products={activeProducts} transactions={transactions} onNewSale={()=>setView('pos')}/>} {view==='pos'&&<PosView products={activeProducts} cart={cart} payment={payment} note={note} editing={editing} saving={saving} productQuery={productQuery} discountType={discountType} discountValue={discountValue} cashReceived={cashReceived} onProductQuery={setProductQuery} onAdd={addToCart} onQty={changeQty} onRemove={id=>setCart(c=>c.filter(i=>i.productId!==id))} onPayment={v=>{setPayment(v);if(v!=='Tunai')setCashReceived(0)}} onNote={setNote} onDiscountType={setDiscountType} onDiscountValue={setDiscountValue} onCashReceived={setCashReceived} onCheckout={checkout} onCancelEdit={resetSale}/>} {view==='inventory'&&isAdmin&&<InventoryView products={activeProducts} onAddProduct={()=>setAddProductOpen(true)} onEditProduct={openProductEditor} onStockIn={p=>{setStockOpen(p);setStockQty(1);setStockNote('Restock')}} onDeleteProduct={archiveProduct}/>} {view==='reports'&&isAdmin&&<ReportsView products={products} transactions={transactions} query={query} dateFilter={dateFilter} onQuery={setQuery} onDateFilter={setDateFilter} onExport={rows=>exportTransactionsCsv(rows,productMap)} onView={tx=>{setReceiptCash(null);setReceipt(tx)}} onEdit={editTransaction} onDelete={deleteTransaction}/>}</section>{isAdmin&&addProductOpen&&<AddProductModal value={newProduct} saving={saving} onChange={setNewProduct} onClose={()=>setAddProductOpen(false)} onSave={addProduct}/>} {isAdmin&&editProductOpen&&<EditProductModal product={editProductOpen} value={editProductValue} saving={saving} onChange={setEditProductValue} onClose={()=>setEditProductOpen(null)} onSave={saveProductEdit}/>} {isAdmin&&stockOpen&&<StockInModal product={stockOpen} quantity={stockQty} note={stockNote} saving={saving} onQuantity={setStockQty} onNote={setStockNote} onClose={()=>setStockOpen(null)} onSave={stockIn}/>} {receipt&&<ReceiptModal receipt={receipt} products={products} cashReceived={receiptCash?.received} changeDue={receiptCash?.change} onClose={closeReceipt} onNewSale={newSaleFromReceipt}/>}</main>}
